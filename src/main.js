import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

// createApp(App).mount('#app')

let app = createApp(App)
app.mount("#app") // maybe export VM and

// console.log(app)